# TrackIt — Caddy reverse proxy config
# Append this to the server's /etc/caddy/Caddyfile

trackit-demo.duckdns.org {
    reverse_proxy localhost:3000

    # Logs (opsiyonel)
    log {
        output file /var/log/caddy/trackit.log
        format console
    }
}
