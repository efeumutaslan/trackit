services:
  app:
    build: .
    container_name: trackit
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - trackit_data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/trackit.db

volumes:
  trackit_data:
